# ParticleNet (DynamicEdgeConv) for quark/gluon classification
experiment_name: particlenet
model: particlenet
data_root: data
k: 16

# Model
in_channels: 3

# Training
batch_size: 128
learning_rate: 1.0e-3
min_lr: 1.0e-5
weight_decay: 1.0e-4
num_epochs: 50
patience: 10
seed: 42
